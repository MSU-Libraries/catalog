tcp:
  routers:
    mariadb-router:
      service: mariadb
      rule: "HostSNI(`*`)"
      entrypoints: msul-db-ent
  services:
    mariadb:
      weighted:
        services:
        - name: msul-galera1-app@docker
          weight: 1
        - name: msul-galera2-app@docker
          weight: 1
        - name: msul-galera3-app@docker
          weight: 1
