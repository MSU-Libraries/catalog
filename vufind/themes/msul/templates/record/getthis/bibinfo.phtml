<!-- Get-this: Bibinfo -->
<div>
  <b><?=$this->escapeHtml($this->driver->getRawData()['title_full'] ?? '') ?></b><br/>
  <br/>
  <p class="text-left">
    Status:
    <?php
	$status = "";
    foreach ($this->getthis->items as $item) {
        $status .= $this->escapeHtml($item['status'] . ' | ');
    }
	echo substr($status,0, -2);
    ?>
  </p><br/>
</div>
